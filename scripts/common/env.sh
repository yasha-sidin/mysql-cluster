#!/usr/bin/env bash

MYSQL_ROOT_PASSWORD="${MYSQL_ROOT_PASSWORD:-RootPassw0rd!}"
MYSQL_APP_USER="${MYSQL_APP_USER:-app}"
MYSQL_APP_PASSWORD="${MYSQL_APP_PASSWORD:-AppPassw0rd!}"
MYSQL_MONITOR_USER="${MYSQL_MONITOR_USER:-monitor}"
MYSQL_MONITOR_PASSWORD="${MYSQL_MONITOR_PASSWORD:-MonitorPassw0rd!}"
MYSQL_BACKUP_USER="${MYSQL_BACKUP_USER:-backup}"
MYSQL_BACKUP_PASSWORD="${MYSQL_BACKUP_PASSWORD:-BackupPassw0rd!}"
BACKUP_WORKDIR="${BACKUP_WORKDIR:-/backup-work}"
MINIO_ACCESS_KEY="${MINIO_ACCESS_KEY:-minioadmin}"
MINIO_SECRET_KEY="${MINIO_SECRET_KEY:-minioadmin123}"
BACKUP_BUCKET="${BACKUP_BUCKET:-mysql-backups}"
MINIO_A_ENDPOINT="${MINIO_A_ENDPOINT:-http://minio-a:9000}"
MINIO_B_ENDPOINT="${MINIO_B_ENDPOINT:-http://minio-b:9000}"
CLUSTER_NAME="${CLUSTER_NAME:-otusCluster}"
MYSQL_NODES="${MYSQL_NODES:-db1 db2 db3 db4 db5}"
PROXYSQL_NODES="${PROXYSQL_NODES:-proxysql1 proxysql2}"
